export enum SegmentStatusEnum{
    APPROVED = 'APPROVED',
    REJECTED = 'REJECTED',
    AUDIT = 'AUDIT',
    RETURNED = 'RETURNED',
    PENDING = 'PENDING',
}

export enum SegmentStatusUpdateEnum{
    APPROVED = 'APPROVED',
    AUDIT = 'AUDIT',
    RETURNED = 'RETURNED',
}